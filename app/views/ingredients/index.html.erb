<h1>Ingredients</h1>

<% if @ingredients.empty? %>
  <p>There are no ingredients.</p>
<% else %>
  <ul>
    <% @ingredients.each do |ingredient| %>
      <li><%= ingredient.name %></li>
    <% end %>
  </ul>
<% end %>